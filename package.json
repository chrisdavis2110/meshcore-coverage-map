{
  "name": "meshcore-coverage-map",
  "version": "1.0.0",
  "private": true,
  "description": "MeshCore Coverage Map - Self-hosted version",
  "scripts": {
    "docker:dev": "cd server && npm run docker:dev",
    "docker:dev:detached": "cd server && npm run docker:dev:detached",
    "docker:down": "cd server && npm run docker:down",
    "docker:logs": "cd server && npm run docker:logs",
    "docker:prod": "cd server && npm run docker:prod",
    "docker:prod:detached": "cd server && npm run docker:prod:detached"
  }
}

